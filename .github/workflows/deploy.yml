name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: 3.x

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_PRIVATE_KEY }}
          script: |
            echo "$PRIVATE_KEY_CONTENT" > /Users/abdurrahmanabulhossain/Documents/My-Projects/Stock-screener-app.pem
            cat /Users/abdurrahmanabulhossain/Documents/My-Projects/Stock-screener-app.pem
            ssh -i /Users/abdurrahmanabulhossain/Documents/My-Projects/Stock-screener-app.pem ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }} "echo Test SSH"

            cd /home/ec2-user/Projects/Stock-Screener-Backend-FastAPI
            git pull origin main  # Update my code
            docker-compose down   # Stop existing containers
            docker-compose build  # Build the updated Docker image
            docker-compose up -d  # Start new containers
